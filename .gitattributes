# Set the default behavior for all text files
* text=auto

# Force shell scripts to always use LF (Linux) line endings
*.sh eol=lf

# Force batch scripts to always use CRLF (Windows) line endings
*.bat eol=crlf